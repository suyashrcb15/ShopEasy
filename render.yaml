services:
  - type: web
    name: ecom-proj
    env: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    rootDir: .
    envVars:
      - key: JAVA_VERSION
        value: 21
    healthCheckPath: /
